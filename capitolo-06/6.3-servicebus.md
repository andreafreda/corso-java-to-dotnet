# 6.3 Messaggistica asincrona con Azure Service Bus

> **Analogia Java**: equivalente di Spring JMS / Spring Cloud Azure Service Bus

---

## Introduzione

`OrderService` conferma un ordine e deve notificarlo ad altri servizi — `InventoryService` deve scalare il magazzino, `NotificationService` deve inviare l'email al cliente. Chiamarli in modo sincrono accoppia i servizi: se `InventoryService` è down, l'ordine non viene confermato. La soluzione è la messaggistica asincrona: `OrderService` pubblica un evento su Azure Service Bus e i consumer lo processano indipendentemente.

Questo paragrafo mostra come sviluppare un **producer** e un **consumer** con Azure Service Bus in .NET.

---

## Come si faceva in Java

```java
// Spring JMS — producer
@Service
public class OrderEventPublisher {

    private final JmsTemplate jmsTemplate;

    public void publishOrderCreated(Order order) {
        jmsTemplate.convertAndSend("order-events",
            new OrderCreatedEvent(order.getId(), order.getCustomer(), order.getTotal()));
    }
}
```

```java
// Spring JMS — consumer
@Component
public class OrderEventConsumer {

    @JmsListener(destination = "order-events")
    public void handleOrderCreated(OrderCreatedEvent event) {
        log.info("Ordine ricevuto: {}", event.orderId());
        inventoryService.decrementStock(event.orderId());
    }
}
```

```yaml
# application.yml
spring:
  jms:
    servicebus:
      connection-string: ${AZURE_SERVICEBUS_CONNECTION_STRING}
      pricing-tier: standard
```

---

## Azure Service Bus in locale con Docker

Per sviluppo locale usiamo **Azurite** — l'emulatore ufficiale Microsoft per i servizi Azure Storage e Service Bus:

```yaml
# docker-compose.yml — aggiungi il servizio
services:
  azuresql:
    image: mcr.microsoft.com/azure-sql-edge:latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Password123!"
    ports:
      - "1433:1433"

  servicebus:
    image: mcr.microsoft.com/azure-messaging/servicebus-emulator:latest
    environment:
      ACCEPT_EULA: "Y"
    ports:
      - "5672:5672"    # AMQP
      - "9090:9090"    # management
    volumes:
      - ./servicebus-config.json:/ServiceBus_Emulator/ConfigFiles/Config.json
      # Il file servicebus-config.json va nella root del progetto (accanto a docker-compose.yml)
```

```json
// servicebus-config.json — definisce namespace, code e topic
{
  "UserConfig": {
    "Namespaces": [
      {
        "Name": "orderservice-ns",
        "Queues": [
          { "Name": "order-events" }
        ],
        "Topics": [
          {
            "Name": "order-created",
            "Subscriptions": [
              { "Name": "inventory-sub" },
              { "Name": "notification-sub" }
            ]
          }
        ]
      }
    ]
  }
}
```

```bash
docker compose up -d
```

---

## Installazione SDK

```bash
dotnet add OrderService.Infrastructure package Azure.Messaging.ServiceBus
```

---

## Evento di dominio e interfaccia publisher

```csharp
// Core/Events/OrderCreatedEvent.cs
namespace OrderService.Core.Events;

public record OrderCreatedEvent(
    int    OrderId,
    string Customer,
    decimal Total,
    DateTime CreatedAt);
```

```csharp
// Core/Interfaces/IOrderEventPublisher.cs
namespace OrderService.Core.Interfaces;

public interface IOrderEventPublisher
{
    Task PublishOrderCreatedAsync(OrderCreatedEvent evt, CancellationToken ct = default);
}
```

Il `Core` dipende da `IOrderEventPublisher` — non sa nulla di Azure Service Bus. L'implementazione concreta (`OrderEventPublisher`) sta in Infrastructure e viene iniettata tramite DI.

---

## Producer — pubblicare un messaggio

> 📝 **Code Step 13** — Aggiungi `OrderEventPublisher` e pubblica l'evento alla conferma dell'ordine

```
OrderService.Infrastructure/
└── Messaging/
    └── OrderEventPublisher.cs    ← nuovo
```

```csharp
// Infrastructure/Messaging/OrderEventPublisher.cs
using System.Text.Json;
using Azure.Messaging.ServiceBus;
using Microsoft.Extensions.Logging;
using OrderService.Core.Events;

namespace OrderService.Infrastructure.Messaging;

public class OrderEventPublisher : IOrderEventPublisher
{
    private readonly ServiceBusSender _sender;
    private readonly ILogger<OrderEventPublisher> _logger;

    public OrderEventPublisher(ServiceBusClient client, ILogger<OrderEventPublisher> logger)
    {
        _sender = client.CreateSender("order-created");
        _logger = logger;
    }

    public async Task PublishOrderCreatedAsync(OrderCreatedEvent evt, CancellationToken ct = default)
    {
        var body    = JsonSerializer.Serialize(evt);
        var message = new ServiceBusMessage(body)
        {
            ContentType     = "application/json",
            Subject         = "OrderCreated",
            MessageId       = $"order-{evt.OrderId}-{Guid.NewGuid()}"
        };

        await _sender.SendMessageAsync(message, ct);
        _logger.LogInformation("Evento OrderCreated pubblicato per OrderId={OrderId}", evt.OrderId);
    }
}
```

### Registrazione

```csharp
// Infrastructure/DependencyInjection.cs
services.AddSingleton(new ServiceBusClient(
    configuration.GetConnectionString("ServiceBus")));
services.AddScoped<IOrderEventPublisher, OrderEventPublisher>();
```

```json
// appsettings.Development.json
{
  "ConnectionStrings": {
    "Default": "Server=localhost,1433;Database=OrderDb;User Id=sa;Password=Password123!;TrustServerCertificate=True",
    "ServiceBus": "Endpoint=sb://localhost;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=SAS_KEY_VALUE;UseDevelopmentEmulator=true;"
  }
}
```

> `UseDevelopmentEmulator=true` disabilita l'autenticazione reale — con l'emulatore locale qualsiasi valore per `SharedAccessKey` funziona. `SAS_KEY_VALUE` non è una chiave da trovare: è letteralmente il placeholder che puoi lasciare invariato.

### Usa il publisher nel service

```csharp
// Core/Services/OrderService.cs — aggiorna costruttore e CreateAsync
public class OrderService : IOrderService
{
    private readonly IOrderRepository      _repository;
    private readonly IOrderEventPublisher  _publisher;   // ← interfaccia, non implementazione

    public OrderService(IOrderRepository repository, IOrderEventPublisher publisher)
    {
        _repository = repository;
        _publisher  = publisher;
    }

    public async Task<Order> CreateAsync(CreateOrderRequest request, CancellationToken ct = default)
    {
        var order = new Order
        {
            Customer  = request.Customer,
            Total     = request.Total,
            Status    = OrderStatus.Pending,
            CreatedAt = DateTime.UtcNow
        };

        await _repository.AddAsync(order, ct);

        await _publisher.PublishOrderCreatedAsync(new OrderCreatedEvent(
            order.Id, order.Customer, order.Total, order.CreatedAt), ct);

        return order;
    }
}
```

---

## Consumer — ricevere messaggi

Il consumer gira come **hosted service** — un processo in background che resta in ascolto sulla subscription. In .NET si implementa con `IHostedService` o `BackgroundService`.

```
OrderService.Infrastructure/
└── Messaging/
    ├── OrderEventPublisher.cs
    └── OrderCreatedConsumer.cs    ← nuovo
```

```csharp
// Infrastructure/Messaging/OrderCreatedConsumer.cs
using System.Text.Json;
using Azure.Messaging.ServiceBus;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Hosting;
using Microsoft.Extensions.Logging;
using OrderService.Core.Events;

namespace OrderService.Infrastructure.Messaging;

public class OrderCreatedConsumer : BackgroundService
{
    private readonly ServiceBusProcessor _processor;
    private readonly IServiceScopeFactory _scopeFactory;
    private readonly ILogger<OrderCreatedConsumer> _logger;

    public OrderCreatedConsumer(
        ServiceBusClient client,
        IServiceScopeFactory scopeFactory,
        ILogger<OrderCreatedConsumer> logger)
    {
        _processor  = client.CreateProcessor("order-created", "inventory-sub");
        _scopeFactory = scopeFactory;
        _logger     = logger;
    }

    protected override async Task ExecuteAsync(CancellationToken ct)
    {
        _processor.ProcessMessageAsync += HandleMessageAsync;
        _processor.ProcessErrorAsync   += HandleErrorAsync;

        await _processor.StartProcessingAsync(ct);

        // Resta in ascolto fino alla cancellazione
        await Task.Delay(Timeout.Infinite, ct).ConfigureAwait(ConfigureAwaitOptions.SuppressThrowing);

        await _processor.StopProcessingAsync();
    }

    private async Task HandleMessageAsync(ProcessMessageEventArgs args)
    {
        var body = args.Message.Body.ToString();
        var evt  = JsonSerializer.Deserialize<OrderCreatedEvent>(body);

        if (evt is null)
        {
            await args.DeadLetterMessageAsync(args.Message, "DeserializationFailed", "Body non valido");
            return;
        }

        _logger.LogInformation("Elaborazione OrderCreated per OrderId={OrderId}", evt.OrderId);

        // IServiceScopeFactory necessario perché BackgroundService è Singleton
        // ma i servizi di dominio sono Scoped
        using var scope = _scopeFactory.CreateScope();

        // In un microservizio reale risolveresti qui il tuo servizio di dominio, es.:
        // var inventoryService = scope.ServiceProvider.GetRequiredService<IInventoryService>();
        // await inventoryService.DecrementStockAsync(evt.OrderId, args.CancellationToken);
        //
        // Questo consumer è nel progetto OrderService solo a scopo dimostrativo —
        // in produzione, InventoryService avrebbe il proprio consumer con la propria logica.
        await Task.CompletedTask;

        await args.CompleteMessageAsync(args.Message);
    }

    private Task HandleErrorAsync(ProcessErrorEventArgs args)
    {
        _logger.LogError(args.Exception,
            "Errore nel processing Service Bus: {ErrorSource}", args.ErrorSource);
        return Task.CompletedTask;
    }
}
```

### Registrazione del consumer

```csharp
// Infrastructure/DependencyInjection.cs
services.AddHostedService<OrderCreatedConsumer>();
```

---

## Queue vs Topic

L'esempio usa un **topic** con subscriptions — un messaggio pubblicato su `order-created` viene consegnato a tutti i subscriber (`inventory-sub`, `notification-sub`). Ogni subscriber riceve una copia indipendente del messaggio.

```
Publisher → Topic "order-created"
                ├── Subscription "inventory-sub"  → InventoryService consumer
                └── Subscription "notification-sub" → NotificationService consumer
```

Se hai un solo consumer, usa una **queue**: più semplice, nessuna subscription da gestire.

```csharp
// Queue — un solo consumer
_sender    = client.CreateSender("order-events");           // producer
_processor = client.CreateProcessor("order-events");        // consumer
```

---

## Spiegazione

**`BackgroundService` e `IServiceScopeFactory`** — `BackgroundService` è registrato come `Singleton`. I servizi di dominio (`IOrderRepository`, ecc.) sono `Scoped`. Non puoi iniettarli direttamente in un Singleton — otterresti un'eccezione a runtime. La soluzione è `IServiceScopeFactory`: crei uno scope per ogni messaggio e risolvi i servizi Scoped da quello scope.

**`CompleteMessageAsync` e `DeadLetterMessageAsync`** — Service Bus usa un modello di acknowledgment esplicito. Se non chiami `CompleteMessageAsync`, il messaggio torna in coda dopo il lock timeout e verrà ri-consegnato. Se il processing fallisce troppe volte, Service Bus sposta il messaggio nella **dead-letter queue** — una coda separata per i messaggi non processabili.

**`MessageId` idempotente** — impostare un `MessageId` univoco per ogni messaggio permette a Service Bus di deduplicare i messaggi duplicati se hai abilitato la duplicate detection sul topic. Buona pratica anche senza deduplicazione — aiuta il debugging.

**`IOrderEventPublisher` nel Core** — `OrderService.Core` non dipende da `Azure.Messaging.ServiceBus`. Il Core definisce l'interfaccia `IOrderEventPublisher`, l'implementazione concreta (`OrderEventPublisher`) sta in Infrastructure e viene registrata con `AddScoped<IOrderEventPublisher, OrderEventPublisher>()`. Nei test puoi sostituirla con un mock senza toccare il Service Bus reale.

---

## Confronto con Java

| Spring JMS / Spring Cloud Azure | ASP.NET Core |
|---|---|
| `JmsTemplate.convertAndSend()` | `ServiceBusSender.SendMessageAsync()` |
| `@JmsListener(destination = "queue")` | `ServiceBusProcessor` in `BackgroundService` |
| `@JmsListener` su metodo — Spring gestisce il thread | `ProcessMessageAsync` event handler |
| Dead letter automatico su eccezione non gestita | `DeadLetterMessageAsync()` esplicito |
| Topic + subscription | Topic + subscription — stesso concetto |
| `MessageConverter` per serializzazione | `JsonSerializer.Serialize/Deserialize` manuale |
| `spring.jms.servicebus.connection-string` | `ConnectionStrings:ServiceBus` in `appsettings.json` |

---

*Prossimo paragrafo → [6.4 Outbox pattern: garantire consistenza distribuita](./6.4-outbox.md)*
# 6.1 HttpClient e IHttpClientFactory

> **Analogia Java**: equivalente di `RestTemplate` / `WebClient`

---

## Introduzione

`OrderService` deve chiamare un altro microservizio — `InventoryService` — per verificare la disponibilità prima di confermare un ordine. In Java usavi `RestTemplate` (sincrono) o `WebClient` (reattivo). In .NET il client HTTP è `HttpClient`, ma non va istanziato direttamente: la gestione del ciclo di vita è la trappola più comune per i developer .NET. La soluzione è `IHttpClientFactory`.

---

## Come si faceva in Java

```java
// RestTemplate — il modo classico
@Service
public class InventoryClient {

    private final RestTemplate restTemplate;

    public InventoryClient(RestTemplateBuilder builder) {
        this.restTemplate = builder
            .rootUri("http://inventory-service")
            .build();
    }

    public boolean isAvailable(int productId, int quantity) {
        return restTemplate.getForObject(
            "/inventory/{id}/available?quantity={qty}",
            Boolean.class,
            productId, quantity);
    }
}
```

```java
// WebClient — il modo reattivo/moderno
@Service
public class InventoryClient {

    private final WebClient webClient;

    public InventoryClient(WebClient.Builder builder) {
        this.webClient = builder
            .baseUrl("http://inventory-service")
            .build();
    }

    public Mono<Boolean> isAvailable(int productId, int quantity) {
        return webClient.get()
            .uri("/inventory/{id}/available?quantity={qty}", productId, quantity)
            .retrieve()
            .bodyToMono(Boolean.class);
    }
}
```

---

## Il problema con `new HttpClient()`

```csharp
// ❌ MAI fare così — esaurisce le connessioni TCP disponibili
public async Task<bool> IsAvailableAsync(int productId, int quantity)
{
    using var client = new HttpClient();  // crea e distrugge una connessione a ogni chiamata
    var response = await client.GetAsync($"http://inventory-service/inventory/{productId}/available?quantity={quantity}");
    return response.IsSuccessStatusCode;
}
```

`HttpClient` mantiene le connessioni TCP aperte internamente. Creare e distruggere istanze con `using` non chiude le connessioni immediatamente — le lascia in stato `TIME_WAIT`, esaurendo i socket disponibili sotto carico. Questo è il **socket exhaustion**, il bug di produzione più comune con `HttpClient`.

---

## `IHttpClientFactory` — la soluzione corretta

> 📝 **Code Step 12** — Aggiungi `InventoryClient` e registra `IHttpClientFactory`

`IHttpClientFactory` gestisce un pool di connessioni `HttpMessageHandler` — le connessioni vengono riusate correttamente e ruotate periodicamente per rispettare i cambiamenti DNS.

### 1. Interfaccia nel Core

```
OrderService.Core/
└── Interfaces/
    └── IInventoryClient.cs    ← nuovo
```

```csharp
// Core/Interfaces/IInventoryClient.cs
namespace OrderService.Core.Interfaces;

public interface IInventoryClient
{
    Task<bool> IsAvailableAsync(int productId, int quantity, CancellationToken ct = default);
}
```

### 2. `InventoryClient` — typed client in Infrastructure

```
OrderService.Infrastructure/
└── Http/
    └── InventoryClient.cs    ← nuovo
```

```csharp
// Infrastructure/Http/InventoryClient.cs
using System.Net.Http.Json;
using Microsoft.Extensions.Logging;
using OrderService.Core.Interfaces;

namespace OrderService.Infrastructure.Http;

public class InventoryClient : IInventoryClient
{
    private readonly HttpClient _client;
    private readonly ILogger<InventoryClient> _logger;

    // HttpClient viene iniettato già configurato da IHttpClientFactory — nessuna stringa magica
    public InventoryClient(HttpClient client, ILogger<InventoryClient> logger)
    {
        _client = client;
        _logger = logger;
    }

    public async Task<bool> IsAvailableAsync(int productId, int quantity, CancellationToken ct = default)
    {
        try
        {
            var response = await _client.GetAsync(
                $"/inventory/{productId}/available?quantity={quantity}", ct);

            response.EnsureSuccessStatusCode();
            return await response.Content.ReadFromJsonAsync<bool>(ct);
        }
        catch (HttpRequestException ex)
        {
            _logger.LogWarning(ex, "InventoryService non raggiungibile per productId={ProductId}", productId);
            return false;  // fallback: assume non disponibile
        }
    }
}
```

### 2. Registrazione in `AddInfrastructure`

```csharp
// Infrastructure/DependencyInjection.cs
public static IServiceCollection AddInfrastructure(
    this IServiceCollection services,
    IConfiguration configuration)
{
    services.AddDbContext<AppDbContext>(options =>
        options.UseSqlServer(configuration.GetConnectionString("Default")));
    // PostgreSQL: sostituisci UseSqlServer con UseNpgsql

    services.AddScoped<IOrderRepository, EfCoreOrderRepository>();

    // Typed client — AddHttpClient<T> registra sia InventoryClient che IInventoryClient
    services.AddHttpClient<IInventoryClient, InventoryClient>(client =>
    {
        client.BaseAddress = new Uri(configuration["Services:InventoryUrl"]!);
        client.DefaultRequestHeaders.Add("Accept", "application/json");
    });

    return services;
}
```

```json
// appsettings.Development.json
{
  "ConnectionStrings": {
    "Default": "Server=localhost,1433;Database=OrderDb;User Id=sa;Password=Password123!;TrustServerCertificate=True"
  },
  "Services": {
    "InventoryUrl": "http://localhost:5001"
  }
}
```

### 4. Aggiorna `CreateOrderRequest` con i campi prodotto

```csharp
// Core/Requests/CreateOrderRequest.cs — aggiornato
public record CreateOrderRequest(
    string  Customer,
    decimal Total,
    int     ProductId,   // ← aggiunto
    int     Quantity);   // ← aggiunto
```

### 5. Aggiorna il costruttore di `OrderService`

```csharp
// Core/Services/OrderService.cs — aggiorna il costruttore
public class OrderService : IOrderService
{
    private readonly IOrderRepository  _repository;
    private readonly IInventoryClient  _inventoryClient;  // ← interfaccia, non implementazione

    public OrderService(IOrderRepository repository, IInventoryClient inventoryClient)
    {
        _repository      = repository;
        _inventoryClient = inventoryClient;
    }

    // ... resto invariato
}
```

### 6. Usa `InventoryClient` in `CreateAsync`

```csharp
// Core/Services/OrderService.cs — aggiorna CreateAsync
public async Task<Order> CreateAsync(CreateOrderRequest request, CancellationToken ct = default)
{
    var available = await _inventoryClient.IsAvailableAsync(request.ProductId, request.Quantity, ct);
    if (!available)
        throw new InvalidOperationException($"Prodotto {request.ProductId} non disponibile");

    var order = new Order
    {
        Customer  = request.Customer,
        Total     = request.Total,
        Status    = OrderStatus.Pending,
        CreatedAt = DateTime.UtcNow
    };

    return await _repository.AddAsync(order, ct);
}
```

---

## Named client — quando serve

Il typed client è il pattern standard. Il **named client** (`factory.CreateClient("nome")`) è utile in casi specifici: quando devi creare più istanze dello stesso tipo con configurazioni diverse, o quando il client è una classe di terze parti che non puoi modificare.

```csharp
// Named client — utile quando servono più configurazioni per lo stesso tipo
services.AddHttpClient("inventory-primary", client =>
    client.BaseAddress = new Uri("http://inventory-service-1"));

services.AddHttpClient("inventory-secondary", client =>
    client.BaseAddress = new Uri("http://inventory-service-2"));

// Uso — devi risolvere IHttpClientFactory esplicitamente
public class InventoryClient
{
    public InventoryClient(IHttpClientFactory factory)
    {
        _primary   = factory.CreateClient("inventory-primary");
        _secondary = factory.CreateClient("inventory-secondary");
    }
}
```

Per `OrderService` il typed client è la scelta corretta: un solo `InventoryClient`, una sola configurazione.

---

## Spiegazione

**Socket exhaustion** — il problema di `new HttpClient()` non è ovvio. Il `using` chiama `Dispose()` ma le connessioni TCP restano in `TIME_WAIT` per ~240 secondi. Sotto carico esaurisci tutti i socket disponibili del processo. `IHttpClientFactory` riusa i `HttpMessageHandler` nel pool — le connessioni vengono mantenute aperte correttamente.

**DNS refresh** — `IHttpClientFactory` ruota i `HttpMessageHandler` ogni 2 minuti per default. Questo garantisce che i cambiamenti DNS (essenziali in Kubernetes e nei deployment rolling) vengano rispettati. Con una singola istanza `HttpClient` statica il DNS verrebbe cachato indefinitamente.

**`ReadFromJsonAsync`** — metodo di estensione di `System.Net.Http.Json` (incluso in .NET 6+). Deserializza il body JSON direttamente sul tipo C# senza `JsonSerializer.Deserialize` esplicito.

**`InvalidOperationException` → 422** — `CreateAsync` ora può lanciare `InvalidOperationException` quando l'inventario non è disponibile. Il `GlobalExceptionHandler` del capitolo 4 mappa solo `NotFoundException` (404) e `ArgumentException` (400) — `InvalidOperationException` cadrebbe nel catch-all 500. Il codice corretto è **422 Unprocessable Entity**: la richiesta è sintatticamente valida ma viola una regola di business. Aggiorna il middleware:

```csharp
// Api/Middleware/ExceptionHandler.cs
var (statusCode, title) = exception switch
{
    NotFoundException         => (StatusCodes.Status404NotFound,            "Resource not found"),
    InvalidOperationException => (StatusCodes.Status422UnprocessableEntity, "Business rule violation"),  // ← aggiunto
    ArgumentException         => (StatusCodes.Status400BadRequest,          "Invalid request"),
    _                         => (StatusCodes.Status500InternalServerError,  "An unexpected error occurred")
};
```

---

## Confronto con Java

| Spring Boot | ASP.NET Core |
|---|---|
| `RestTemplate` | `HttpClient` via `IHttpClientFactory` |
| `RestTemplateBuilder.rootUri()` | `AddHttpClient("name", c => c.BaseAddress = ...)` |
| `WebClient.Builder.baseUrl()` | Typed client con `AddHttpClient<T>()` |
| `@LoadBalanced RestTemplate` | `IHttpClientFactory` + service discovery (6.2) |
| `restTemplate.getForObject(url, T.class)` | `client.GetFromJsonAsync<T>(url)` |
| `restTemplate.postForObject(url, body, T.class)` | `client.PostAsJsonAsync(url, body)` |
| `WebClientResponseException` | `HttpRequestException` |

---

*Prossimo paragrafo → [6.2 Service discovery e pattern client-side](./6.2-service-discovery.md)*